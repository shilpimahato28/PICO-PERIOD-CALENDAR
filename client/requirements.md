## Packages
date-fns | Date manipulation for calendar and cycle calculations
react-day-picker | Calendar component for logging periods
recharts | Charts for cycle visualization
framer-motion | Smooth page transitions and UI animations
lucide-react | Icons (already in base, but listing for completeness)
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
